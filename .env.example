# Project Configuration
PROJECT_NAME=ai-docs-intelligence
ENVIRONMENT=development

# Paths
DATA_DIR=dataset
OUTPUT_DIR=output
MODEL_DIR=models

# OCR Settings
DEFAULT_OCR_ENGINE=paddle
OCR_LANGUAGE=en
OCR_CONFIDENCE_THRESHOLD=0.6

# Logging
LOG_LEVEL=INFO
LOG_FILE=logs/app.log

# API Settings (if needed)
API_HOST=0.0.0.0
API_PORT=8000

# Database (if needed)
DATABASE_URL=sqlite:///./app.db
